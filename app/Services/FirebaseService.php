<?php

namespace App\Services;

class FirebaseService
{

    // public function getAccessToken() {
    //     $scopes = ['https://www.googleapis.com/auth/firebase.messaging'];

    //     $jsonKey = json_decode(file_get_contents(storage_path('rentnking-4049b-firebase-adminsdk-mt9yw-0052663b31.json')), true);
    //     $credentials = new ServiceAccountCredentials($scopes, $jsonKey);

    //     if ($credentials->fetchAuthToken()) {
    //         return $credentials->getLastReceivedToken()['access_token'];
    //     } else {
    //         throw new Exception('Unable to fetch access token');
    //     }
    // }
}
